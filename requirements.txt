# ===== Price Tracker — Desktop App (runtime dependencies) =====
# Build-time tools live in requirements-dev.txt.

# --- Scraping (reused from the current app) ---
selenium>=4.20.0          # headless browser for JS-rendered price/stock
beautifulsoup4>=4.12.0    # HTML parsing
lxml>=5.2.0               # fast parser backend for BeautifulSoup
requests>=2.31.0          # lightweight fetch / fallback when Chrome is unavailable

# --- Desktop UI (replaces Streamlit) ---
PySide6>=6.7.0            # native Qt desktop GUI
pyqtgraph>=0.13.0         # fast, interactive price-history charts

# --- Data, storage & export ---
SQLAlchemy>=2.0.0         # ORM over SQLite (local cache), swappable to Postgres
pandas>=2.0.0             # timescale filtering/resampling + CSV/Excel export
openpyxl>=3.1.0           # Excel (.xlsx) writer used by pandas.to_excel

# --- Accounts & cloud sync (Phase 6) ---
supabase>=2.0.0           # Supabase Auth (email confirmation) + Postgres data
keyring>=24.0.0           # securely remember login (OS credential store)
